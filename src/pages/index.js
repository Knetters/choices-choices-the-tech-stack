import * as React from "react"
import Header from "/components/Header";

const Home = () => {
  return (
    <main>
      <h1>My Gatsby Page</h1>
      <Header />
    </main>
  );
};

export default Home;
